NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Trusted server-side operator id used as the actor for permission-checked admin
# mutations until real Supabase Auth is connected. Defaults to the seeded
# platform-admin from supabase/identity-test-seed.sql when unset.
GAMEDAY_OPERATOR_USER_ID=
